{
  "buildCommand": "cd packages/database && npx prisma generate && cd ../.. && pnpm turbo run build --filter=@carpetart/web",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
